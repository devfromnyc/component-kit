# ecommerce template

Demo store UI. No database.

```
npm install
npm run dev
```

Routes: `/`, `/products`, `/products/[id]`, cart drawer, `/checkout`, `/checkout/thank-you`, `/sign-in`, `/sign-up`.

Strip routes you do not need. Swap `src/lib/demoProducts.js` for a real API later.
